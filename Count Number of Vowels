# 1 This function counts the number of vowels in a given string.
def vowelcount (input):
        count = 0
        for i in range(len(input)):
            if input[i] in 'aeiouAEIOU':
                count += 1
        return count
userInput = input("Type your string to calculate number of vowels:")
print(vowelcount(userInput))

# 2 This function calculates the factorial of a given number.
def Factorial (input):

        fact = 1
        for i in range(1,int(input)+1):
           fact=i*fact
        return fact
userInput = input("Type your number to calculate its factorial:")
print(Factorial(userInput))
